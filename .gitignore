node_modules/
dist/
*.js
*.d.ts
!src/**/*.js
!src/**/*.d.ts
!fixtures/**/*.js
.idea/
.DS_Store
todo.md
# Project-local config dir. Both names: .nib/ is current, .heirloom/ is what
# existing checkouts still have on disk. Dropping the old entry would surface
# every one of those directories — debug logs included — as untracked.
.nib/
.heirloom/
dev-todo.md

# keep private registry config/auth out of the repo
.npmrc
